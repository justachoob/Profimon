
<div class="main">

<% if flash[:notice] %>
    <div class="notice"><%= flash[:notice] %></div>
  <% end %>

<h1>List of Profimon Skills</h1>

<table id="table_of_skills" style="text-align: center; margin: 10px auto;">
  <tr>
    <th width="200px">Profimon Skill</th>
    <th width="200px">Subject</th>
    <th width="200px">Stamina</th>
    <th width="200px">Knowledge</th>
    <th width="200px">Homework</th>
  </tr>


  <% @profimon_skills.each do |s| %>
    <tr>
      <td><%= s.name %></td>
      <td><%= s.subject %></td>
      <td><%= s.stamina %></td>
      <td><%= s.knowledge %></td>
      <td><%= s.homework %></td>
      <td><%= link_to 'Edit', edit_profimon_skill_path(s) %> </td>
      <td><%= link_to 'Delete', s, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>

</table>

 <%= link_to 'New Skill', new_profimon_skill_path, :class => "form_link" %> 
 <%= link_to 'Back', admin_path, :class => "form_link" %> 
</div>