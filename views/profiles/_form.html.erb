<%= form_for(profile) do |f| %>
  <% if profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(profile.errors.count, "error") %> prohibited this profile from being saved:</h2>

      <ul>
      <% profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pname,'Profile name' %><br>
    <%= f.text_field :pname ,class: 'input_field_center'%>
  </div>

  <div class="field">
    <%= f.label :faculty ,'Faculty name'%><br>
    <%= f.select :faculty, [['Applied Sciences','Applied Sciences'],['Arts and Social Sciences','Arts and Social Sciences'],['Beedie School of Business','Beedie School of Business'],
    ['Communication, Art and Technology','Communication, Art and Technology'],['Education', 'Education'],['Environment','Environment']]%>
  </div>

  <div class="actions">
    <%= f.submit "Create Profile",class:"form_button" %>
  </div>
<% end %>
